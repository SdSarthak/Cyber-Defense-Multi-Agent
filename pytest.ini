[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    stress: Stress and load tests (run with --stress flag)
    integration: Integration tests requiring running services
addopts = -v --tb=short
